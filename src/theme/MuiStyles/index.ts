import muiButtons from './muiButtons';
import muiInputs from './muiInputs';
import muiDialogs from './muiDialogs';
import muiAvatars from './muiAvatars';
import muiCards from './muiCards';
import muiPapers from './muiPapers';

const MuiStyles = {
  ...muiButtons,
  ...muiInputs,
  ...muiDialogs,
  ...muiAvatars,
  ...muiCards,
  ...muiPapers,
};

export default MuiStyles;
