import muiButtons from './muiButtons';
import muiInputs from './muiInputs';
import muiDialogs from './muiDialogs';

const MuiStyles = {
  ...muiButtons,
  ...muiInputs,
  ...muiDialogs,
};

export default MuiStyles;
